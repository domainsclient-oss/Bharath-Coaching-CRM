// Re-export from the canonical auth context location.
// Pages importing from @/context/AuthContext will work correctly.
export { useAuth, AuthProvider } from '@/lib/auth-context';
export type { } from '@/lib/auth-context';

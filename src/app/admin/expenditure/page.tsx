
"use client";
import { ModulePlaceholder } from '@/components/layout/module-placeholder';
import { TrendingDown } from 'lucide-react';
export default function ExpenditurePage() {
  return <ModulePlaceholder title="Expenditure" description="Track facility costs, utilities, and vendor payments efficiently." icon={TrendingDown} />;
}

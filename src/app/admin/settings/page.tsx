
"use client";
import { ModulePlaceholder } from '@/components/layout/module-placeholder';
import { Settings } from 'lucide-react';
export default function SettingsPage() {
  return <ModulePlaceholder title="Settings" description="Configure institute parameters, role permissions, and system notifications." icon={Settings} />;
}

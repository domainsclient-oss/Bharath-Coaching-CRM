
"use client";
import { ModulePlaceholder } from '@/components/layout/module-placeholder';
import { FileText } from 'lucide-react';
export default function ExaminationPage() {
  return <ModulePlaceholder title="Examination" description="Offline exam scheduling, hall ticket generation, and result entry management." icon={FileText} />;
}
